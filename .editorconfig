root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4
max_line_length = 100

[*.{json,yml,yaml,md,ts,tsx,js,jsx,css,html}]
indent_size = 2

[*.md]
trim_trailing_whitespace = false

[*.{csproj,props,targets,slnx}]
indent_size = 2

# gofmt decides, and it decides tabs. Saying so here keeps an editor that reads
# this file from fighting the formatter the CI job checks against.
[*.go]
indent_style = tab
indent_size = 4

# And `caddy fmt` decides tabs too, for the one file it owns.
[Caddyfile]
indent_style = tab
indent_size = 4

[*.cs]
# Namespaces, usings, and the shape of a file
csharp_style_namespace_declarations = file_scoped:warning
dotnet_sort_system_directives_first = true
csharp_using_directive_placement = outside_namespace:warning

# var only where the type is on the line already
csharp_style_var_when_type_is_apparent = true:suggestion
csharp_style_var_elsewhere = false:suggestion

# Modern C# the codebase should be written in
csharp_style_expression_bodied_methods = when_on_single_line:suggestion
csharp_style_prefer_primary_constructors = true:suggestion
csharp_prefer_braces = true:warning
csharp_style_prefer_pattern_matching = true:suggestion

# Naming: a constant is PascalCase whoever may see it, and every other private
# field is _camelCase. The constant rule comes first because the first matching
# rule wins — without it, `private const string TypePrefix` would be asked to
# call itself `_typePrefix`, which is a spelling nothing in .NET uses.
dotnet_naming_rule.constants_pascal_cased.symbols = constants
dotnet_naming_rule.constants_pascal_cased.style = pascal
dotnet_naming_rule.constants_pascal_cased.severity = warning
dotnet_naming_symbols.constants.applicable_kinds = field
dotnet_naming_symbols.constants.applicable_accessibilities = *
dotnet_naming_symbols.constants.required_modifiers = const
dotnet_naming_style.pascal.capitalization = pascal_case

dotnet_naming_rule.private_fields_underscored.symbols = private_fields
dotnet_naming_rule.private_fields_underscored.style = underscore_camel
dotnet_naming_rule.private_fields_underscored.severity = warning
dotnet_naming_symbols.private_fields.applicable_kinds = field
dotnet_naming_symbols.private_fields.applicable_accessibilities = private
dotnet_naming_style.underscore_camel.capitalization = camel_case
dotnet_naming_style.underscore_camel.required_prefix = _
